procedure n1iim {
	a = 1 / 2 / 3 / 4 / 5 / 6;
	if ((a != b) && (c != d)) then {
		print a;
		if ((1 == 2) || (9 < 8)) then {
			winter = olympics * 2022;
		} else {
			tripping = 1q2w3e / 100;
		}
		read AftershockS15;
	} else {
		print x;
    }
	b = 1 % 2 % 3 % 4 % 5 % 6;
}
