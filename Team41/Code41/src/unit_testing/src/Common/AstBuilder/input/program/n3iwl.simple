procedure n3iwl {
	procedure = procedure + 1;
	if ((a * b * c == 1) && (if % else % then <= 2)) then {
		print = 98 * 20 + 2;
		while (((and * two) >= 1) || ( 1 <= (and * two))) {
			read read;
			if (0 == 0) then {
				print print;
				while (bob == builder) {
					try = catch + 1;
				}
			} else {
				read reader;
				while ((while + 1) == (while + 1)) {
					while = while * while;
				}
			}
		}
	} else {
		print x;
	}
}
