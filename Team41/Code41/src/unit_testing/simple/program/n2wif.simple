procedure n2wif {
	while (group == 32) {
		if (algorithm < maxFlow) then {
			while (a == x) {
				Hunter = (1 + 1) * (3 + 3) / (5 - 5);
            }
            print x;
		} else {
			while (b == y) {
				Hunter2 = (1 + 2) * ((3 * 4) / (5 + 6));
			}
            print x;
		}
		Monster = x + y * z + p * q;
	}
	print computerlab;
}
